export const SPA_SEO_METADATA_SEARCH_SNIPPET_MARKDOWN = [
  `**메타데이터 정합성**을 맞추고, **검색엔진이 페이지를 더 일관되게 해석할 수 있도록** 정리한 작업입니다.
  
• **페이지별 메타 정보 불일치**로 검색 결과에 각 URL의 성격이 충분히 반영되지 않던 문제를 개선하기 위해, **라우트 단위 메타 관리 구조**를 정비하고 **전역 메타 및 구조화 데이터와의 정합성**을 점검했습니다.

• **SPA 환경**에서 초기 응답 HTML만으로 페이지 맥락 전달이 제한될 수 있는 한계를 보완하기 위해, 크롤러가 **자바스크립트 실행 없이도 읽을 수 있는 초기 텍스트 구조**를 보강했습니다.

• **네이버 검색 스니펫이 메타 설명과 다르게 노출되는 문제**를 개선하기 위해, **스니펫 후보 신호**를 보강하고 **서치어드바이저 재수집**을 통해 반영 변화를 검증했습니다.`,
  `---`,

  `## 1. 문제와 진단`,
  `<br/>`,

  `
문제:

기존에는 **페이지별 메타 정보가 일관되게 관리되지 않아**, 검색엔진이 각 URL의 성격을 안정적으로 해석하기 어려운 상태였습니다. **title, description, canonical** 같은 핵심 메타 정보가 **라우트 단위로 정리돼 있지 않다** 보니, 검색 결과에서 페이지별 메시지가 충분히 분리되지 못할 가능성이 있었습니다. 특히 **전역 메타 정보와 페이지별 메타 정보가 함께 존재하는 구조**에서는, 검색엔진이 어떤 정보를 우선적으로 해석하느냐에 따라 대표 메시지가 흔들릴 여지가 있었습니다.

또한 **SPA 환경 특성상 초기 응답 HTML만으로는 페이지 맥락이 충분히 전달되지 않을 수 있었습니다.** 사용자는 자바스크립트 실행 이후 완성된 화면을 보지만, 크롤러는 **초기 HTML을 먼저 해석**하기 때문에 이 단계에서 텍스트 정보가 부족하면 서비스 주제와 핵심 문맥이 약하게 전달될 수 있었습니다.

**네이버 검색**에서는 **스니펫이 메타 설명과 다르게 노출**되는 경우가 있었고, **meta description만으로는 원하는 문구 반영을 기대하기 어려운 상태**였습니다.

<br/>

진단:

이번 작업의 핵심은 **페이지별 메타데이터 정합성**을 맞춰 검색엔진이 **각 URL을 더 일관되게 해석할 수 있도록 구조를 정리하는 것**이었습니다. 동시에 **초기 HTML 단계에서 읽히는 텍스트 신호**를 보강해, 자바스크립트 실행 이전에도 페이지 주제와 기본 문맥이 전달될 수 있도록 보완할 필요가 있었습니다.

특히 **네이버 검색 스니펫 이슈**에 대해서는 단순히 **meta description 수정만으로는 원하는 문구 반영이 어렵다**고 보고, **메타 태그뿐 아니라 구조화 데이터와 초기 HTML, 본문 노출 문구까지 함께 점검**하는 방향으로 접근했습니다.
`,
  `---`,

  `## 2. 구현/해결`,
  `<br/>`,

  `**구현**은 세 갈래로 나눕니다.

첫째, **페이지별로 다르게 관리되던 메타 정보**로 인해 검색 결과 반영이 불안정하던 문제를 개선하기 위해, 앱 루트에 \`HelmetProvider\`를 적용하고 각 페이지의 \`Helmet\`에서 \`title\`, \`meta description\`, \`canonical\`을 **URL별로 관리**하도록 정리했습니다. 이를 통해 **라우트 단위로 핵심 메타 정보**를 일관되게 적용할 수 있도록 했고, **Open Graph와 JSON-LD**는 \`index.html\`의 전역 메타 영역에서 유지하되 페이지별 메타와 메시지가 어긋나지 않도록 함께 점검해 **메타데이터 정합성**을 맞췄습니다.

아래는 실제로 적용한 형태(예시)입니다.

\`\`\`tsx
// App.tsx
return (
  <HelmetProvider>
    <BrowserRouter>
      <AppRouter />
      <ScrollToTop />
      <ToastContainer toastStyle={{ backgroundColor: "#4d5562" }} />
    </BrowserRouter>
  </HelmetProvider>
);
\`\`\`

\`\`\`tsx
// Pricing.tsx (예시)
return (
  <div ref={contWrapRef} className="contents-wrap">
    <Helmet>
      <title>마음AI 플랫폼 이용요금 완벽 가이드 – 무료부터 프리미엄까지</title>
      <meta
        name="description"
        content="플랫폼 무료 이용, 사용량 기반 과금 구조와 할인 혜택을 확인하세요."
      />
      <link rel="canonical" href="https://maum.ai/pricing" />
    </Helmet>
  </div>
);
\`\`\`

<br/>
<br/>

둘째, **SPA에서도 크롤 가능한 초기 HTML**을 채웠습니다. \`index.html\`의 \`<body>\`에서 \`#root\` **이전**에 \`noscript\` 블록과 **숨김 텍스트 블록**을 두고, 그 안에 \`h1\`, \`p\` 같은 **핵심 문장**을 넣었습니다. 이를 통해 **자바스크립트가 실행되지 않더라도 HTML 소스만으로 페이지와 서비스의 주제가 읽히는 형태**를 만들었고, **초기 응답 HTML이 얇게 보일 수 있는 SPA 구조의 한계**를 보완했습니다.

\`\`\`html
<!-- index.html -->
<body>
  <!-- 네이버 크롤러를 위한 초기 콘텐츠 (숨김 처리) -->
  <noscript>
    <div
      style="position: absolute; left: -9999px; width: 1px; height: 1px; overflow: hidden"
    >
      <h1>마음AI</h1>
      <p>마음AI의 SUDA, MAAL, WoRV가 탑재된 'Physical AI' ...</p>
      <p>마음AI는 Physical AI 전문 기업으로, ...</p>
    </div>
  </noscript>

  <!-- 크롤러가 읽을 수 있는 숨겨진 텍스트 -->
  <div
    style="position: absolute; left: -9999px; width: 1px; height: 1px; overflow: hidden"
    aria-hidden="true"
  >
    <h1>마음AI - Physical AI 플랫폼</h1>
    <p>마음AI의 SUDA, MAAL, WoRV가 탑재된 'Physical AI' ...</p>
  </div>
\`\`\`

<br/>
<br/>

셋째, **네이버 검색 스니펫 최적화**를 위해 **스니펫 후보 신호**를 보강했습니다. **meta description만으로는 원하는 문구가 안정적으로 반영되지 않을 수 있다**고 보고, **숨김 본문 텍스트와 JSON-LD description**에 핵심 문구를 추가했습니다. 이 과정에서 **URL 기준으로 메타 태그, 구조화 데이터, 본문 노출 문구가 서로 어긋나지 않도록 정합성**을 함께 점검했고, 수정 후에는 **네이버 서치어드바이저에서 재수집**을 요청해 **실제 노출 변화**까지 확인했습니다.
`,
  `---`,

  `## 3. 성과`,
  `<br/>`,
  `**성과**로는 **Lighthouse SEO 100점**을 달성했고, **네이버 검색 CTR**이 **2.3%에서 2.7%**로 올랐으며, **검색 유입 클릭 수**는 **약 16% 증가**에 기여한 것으로 보고 있습니다.`,
].join("\n\n");
